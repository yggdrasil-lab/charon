# --- Paths (Absolute paths recommended for production) ---
# OBSIDIAN_VAULT_PATH is the root of your git repo (contains .git and /second-brain)
OBSIDIAN_VAULT_PATH=/opt/charon/vault
RCLONE_CACHE_PATH=/opt/charon/cache/rclone
HOST_SSH_PATH=/opt/charon/config/ssh

# --- Rclone / Google Drive Configuration ---
# Note: GDRIVE_VAULT_PATH is the path inside your Google Drive
GDRIVE_VAULT_PATH="Second Brain/second-brain"
GDRIVE_BACKUP_PATH="Second Brain/Backups"
RCLONE_CONFIG_GDRIVE_TYPE=drive
RCLONE_CONFIG_GDRIVE_SCOPE=drive
# RCLONE_CONFIG_GDRIVE_TOKEN should be the full JSON string from rclone config
RCLONE_CONFIG_GDRIVE_TOKEN='{"access_token":"...","refresh_token":"..."}'

# --- Git Backup Configuration ---
GIT_REPO_URL=git@github.com:your_username/your_repo.git
GIT_USER_EMAIL=your_email@example.com
GIT_USER_NAME="Your Name"
# Identifier for this backup source (e.g., "prod", "dev")
ENV="default-source"
